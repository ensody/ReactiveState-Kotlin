[tool.poetry]
name = "ReactiveState-Kotlin"
version = "0.7.0"
description = "reactivestate"
authors = []

[tool.poetry.dependencies]
python = "^3.6"
mkdocs = "^1.1"
mkdocs-material = "^5.1.1"

[build-system]
requires = ["poetry>=1.0.5"]
build-backend = "poetry.masonry.api"
